# Reference Scene Studio - local environment
# Copy this file to .env.local and fill in values you need.

# Leave empty to run in placeholder mode (no real image generation).
OPENAI_API_KEY=

# Optional: if true, falls back to dall-e-3 when gpt-image-1 fails.
# Off by default.
OPENAI_FALLBACK_DALLE3=false
